{
  "name": "whats-the-sound",
  "version": "1.0.0",
  "private": true,
  "description": "PWA multiplayer MIDI music guessing game",
  "packageManager": "pnpm@10.26.2",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "db:push": "supabase db push",
    "db:diff": "supabase db diff",
    "db:reset": "supabase db reset --linked",
    "up": "supabase start && docker compose up -d --build",
    "up:dev": "supabase start && docker compose -f docker-compose.dev.yml up --build",
    "down": "docker compose down && supabase stop",
    "logs": "docker compose logs -f",
    "reset": "docker compose down && supabase db reset --local && docker compose up -d --build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "next": "^15.5.15",
    "supabase": "^2.2.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "esbuild", "sharp", "supabase"]
  }
}
