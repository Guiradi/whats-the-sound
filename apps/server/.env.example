NODE_ENV=development
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
CORS_ORIGINS=http://localhost:3000

# Filled in TASK-002 (Supabase). Uses the new Supabase API key format:
# - Secret key starts with `sb_secret_` and MUST stay server-only (never exposed to the browser).
# - DATABASE_URL is the Session Pooler connection string (port 5432) with the project password.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
DATABASE_URL=

# Filled in TASK-015 (Daily Sound backend)
DAILY_SEED=
